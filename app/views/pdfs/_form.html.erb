<%= form_for(@pdf) do |f| %>
  <% if @pdf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdf.errors.count, "error") %> prohibited this pdf from being saved:</h2>

      <ul>
      <% @pdf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :title, :placeholder => "Title of PDF" %>
  </div>
  <div class="field">
    <%= f.select :user_id, @users, {:include_blank => 'Select A Tenant'}%>
  </div>
  <div class="field">
    <%= f.select :rental_id, @rentals, {:include_blank => 'Select A Property'} %>
  </div>
  <div class="field">
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit "Upload PDF", :class => "btn btn-primary" %>
  </div>
<% end %>

<%= form_for(@maintenance_request) do |f| %>
  <% if @maintenance_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintenance_request.errors.count, "error") %> prohibited this maintenance_request from being saved:</h2>

      <ul>
      <% @maintenance_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :rental_id %><br />
    <%= f.number_field :rental_id %>
  </div>
  <div class="field">
    <%= f.label :request %><br />
    <%= f.text_area :request %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

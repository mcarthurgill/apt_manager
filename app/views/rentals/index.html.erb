<h1>Listing rentals</h1>

<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Rent</th>
    <th>Tenant</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rentals.each do |rental| %>
  <tr>
    <td><%= rental.name %></td>
    <td><%= rental.rent %></td>
    <td><%= rental.user.name if rental.user %></td>
    <%= render 'shared/rental_links', :rental => rental if current_user && current_user.admin %>
  </tr>
<% end %>
</table>

<br />

